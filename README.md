# revolver

version 0.1

standard HTTP client module to send asynchronous HTTP request.

### dependencies
* Erlang/OTP > 18
* rebar3
* ninenines/gun (automatically included)

### run
```
$ rebar3 shell
```
